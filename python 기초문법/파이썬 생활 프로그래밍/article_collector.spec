# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['article_collector.py'],
             pathex=['D:\\Python_Project\\portfolio\\1. study_coding\\2. book\\1. 기초 개념\\10. 파이썬 생활 프로그래밍'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='article_collector',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True )
